# Python / uv
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/

# Env / secrets
.env
.env.local

# Downloaded audio (never commit)
server/downloads/
downloads/

# Local ledger / working data (DB replaces this later; never commit)
server/data/

# OS
.DS_Store
